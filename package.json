{
  "name": "rm-back",
  "version": "1.0.0",
  "description": "backend for rick and morty app",
  "main": "app.js",
  "scripts": {
    "start": "./node_modules/.bin/netlify-lambda serve src --openssl-legacy-provider",
    "build": "./node_modules/.bin/netlify-lambda build src --openssl-legacy-provider",
    "dev": "nodemon src/api.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "rm-back"
  },
  "author": "Daniel Cruz Aguillón",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
